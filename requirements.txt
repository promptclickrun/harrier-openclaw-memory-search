# Harrier server runtime dependencies.
# Install with: pip install -r requirements.txt
#
# torch provides MPS (Apple Silicon) / CUDA acceleration. On macOS the default
# wheel includes MPS support. On Linux with NVIDIA, install the CUDA build per
# https://pytorch.org/get-started/locally/ .
sentence-transformers>=2.6.0
torch>=2.2.0
numpy>=1.24.0
